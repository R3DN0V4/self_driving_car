/** @typedef {{y: number, x: number, offset: number}} Point */

/** @typedef {Array<Point>} PointArray */

/** @typedef {Array<Array<Point>>} PointMatrix */

/** @typedef {Array<Omit<Point, 'offset'>>} XYArray */

/** @typedef {Array<Array<Omit<Point, 'offset'>>>} XYMatrix */
